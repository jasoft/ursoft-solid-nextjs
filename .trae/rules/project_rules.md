# 所有的文字必须都放到 content.ts 文件中, 不得有任何 hard-coded 的字符串出现在任何其他文件中。
# 所有图片必须下载到本地的 public 目录中, 不许引用外链
# 关于测试服务器
交付前用浏览器打开 localhost:3000, 必须确定没有错误, 不要 npm run dev 运行多余的测试服务器, 我已经启动了一个测试服务器, 请不要重复启动, 如果一定要启动, 先运行: pkill -f "next dev"
# 用中文和我对话